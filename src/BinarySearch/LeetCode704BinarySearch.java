package BinarySearch;

public class LeetCode704BinarySearch {
    public static void main(String[] args) {
     /*   class Solution {
            public int search(int[] nums, int target) {
                int n = nums.length;
                int low =0;
                int high =n-1;
                while(low<=high){
                    int mid = low + (high-low)/2;//This is important
                    if(nums[mid]==target){
                        return mid;
                    }
                    else if(nums[mid]>target){
                        high = mid-1;
                    }
                    else if(nums[mid]<target){
                        low = mid+1;
                    }
                }

                return -1;
            }
        }*/
        /*
        Jab bade bade arrays lete hain us time ,
        mid = low + (high-low)/2 lena hota hai
        Reason is explained in notebook
         */
    }
}
