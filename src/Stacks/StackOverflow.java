package Stacks;

public class StackOverflow {
/*
If stack is full and we are trying to push elements then st.push() ->Error
But we haave read that the size of stack is unlimited but stack overflow comes when:-
a. Actual Memory is Full
b. Implementation of Stacks using Arrays (Fixed  size)
*/
}
